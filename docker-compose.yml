# -------------------------------------------------------------------------------------
# This file is used for local development.
# -------------------------------------------------------------------------------------

version: '2'
services:

  nw-chrome:
    container_name: nw-chrome
    image: rhinogram/selenium-chrome:latest
    ports:
      - 4444:4444
    command: yarn canary
    environment:
      - APP_URL=https://dev.dev-rhinogram.com
      - SELENIUM_PORT=4444
      - BROWSER_NAME=chrome
      - HEADLESS=true
    volumes:
      - .:/home/selenium
